#include "main.h"

/**
 * main - _islower - to check if character is lowercase
 * @c: a character argument
 * Description: checks for lowercase character
 * Return: returns 1 and 0
 */

int main(void)
{
	return (c >= 'a' && c <= 'z');
}
